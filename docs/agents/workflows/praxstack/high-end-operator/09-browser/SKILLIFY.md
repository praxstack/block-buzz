# Skillify last scrape

```text
Run /skillify (or /gstack-skillify). Read that skill.

Persist the most recent successful /scrape flow as a browser-skill (script + test + fixture).

Do not scrape a new URL in this turn unless the skill requires a replay.
```
