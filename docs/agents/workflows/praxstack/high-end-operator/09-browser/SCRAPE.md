# Scrape (read-only)

```text
Run /scrape (or /gstack-scrape if ./setup --prefix is on). Read that skill.

URL: [page]
Intent: [what to extract]

Read-only. First matching intent may prototype via $B. Later matches should use the codified browser-skill.

Do not fill forms or submit. That is not scrape. Do not /skillify unless I ask after this extract works.
```
