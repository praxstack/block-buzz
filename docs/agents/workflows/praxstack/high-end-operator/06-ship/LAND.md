# Land and deploy

```text
Run /land-and-deploy (or /gstack-land-and-deploy). Read that skill.

PR: [number or URL]

Merge, wait for CI and deploy, then canary. Do not skip the skill's health checks.

If deploy config is missing, stop and say to run /setup-deploy first.
```
