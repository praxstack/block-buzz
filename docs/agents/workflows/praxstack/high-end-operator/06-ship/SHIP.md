# Ship

```text
Run /ship (or /gstack-ship). Read that skill.

The work is ready for a PR. Follow the skill: tests, review, version, changelog, commit, push, PR.

Do not merge. Do not land-and-deploy unless I ask after the PR exists.

If tests fail, stop and report. Do not force the PR.
```
