# Execute an existing plan

```text
Plan file: [path]

If subagents are available, use subagent-driven-development.
Otherwise use executing-plans.
Read the skill. Do not invent a loop.

Execute the next unfinished task only, unless I say "continue until the plan is done."

Each task: failing test first (test-driven-development), then the slice, then mark the checkbox.

Do not finish the branch or open a PR unless I say so. That is finishing-a-development-branch / /ship.
```
