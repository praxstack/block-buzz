# Pre-landing review

```text
Run /review (or /gstack-review if the prefix is on). Read and follow that skill.

Base: [main / the branch / the commit I name]

Review the current diff. Do not implement fixes unless I ask after the report.

Do not run /plan-eng-review. That is plan mode. This is a code review.

End with the skill's report and a ship / hold verdict.
```
