# QA report only

```text
Run /qa-only (or /gstack-qa-only). Read that skill.

Target: [URL or local path or "none"]

Report only. Do not fix bugs. Do not switch to /qa. Do not run plan reviews.

If there is no UI, use the closest substitute the skill allows (tests, curl) and say what you could not click.

End with health, repro steps, evidence, and the report path.
```
