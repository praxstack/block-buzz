# Investigate

```text
Run /investigate (or /gstack-investigate). Read that skill.

Symptom: [what I saw]
When: [first noticed]
Evidence: [error, log, screenshot path, or "none"]

Do not guess a fix before the skill's investigate / analyze / hypothesize steps. Do not start /qa unless the skill says to reproduce in a browser.
```
