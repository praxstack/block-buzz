# Writing plans

```text
Use writing-plans (superpowers). Read that SKILL.md.

Spec or requirements: [path]

Write the implementation plan. Do not touch application code. Do not run /build.

Save the plan where the skill says (usually docs/superpowers/plans/YYYY-MM-DD-<name>.md).

End with the plan path and the two execution options the skill names (subagent-driven-development vs executing-plans).
```
