# Spec

```text
Run /spec (or /gstack-spec if the prefix is on). Read and follow that skill.

Intent: [what should exist when we are done]
Why now: [one sentence]
Out of scope: [bullets]

Do not implement. Do not start /plan-ceo-review or /autoplan unless I ask after the spec exists.

End with the path of the spec or issue the skill created.
```
